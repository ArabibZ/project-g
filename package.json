{
  "name": "project-g",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "bun --cwd apps/web dev",
    "dev:worker": "bun --cwd apps/worker dev",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "bun --cwd packages/shared typecheck && bun --cwd apps/worker typecheck && bun --cwd apps/web typecheck",
    "test": "bun test",
    "build": "bun --cwd apps/worker build && bun --cwd apps/web build",
    "check": "bun run lint && bun run typecheck && bun run test && bun run build"
  },
  "devDependencies": {
    "@eslint/js": "9.39.5",
    "@types/node": "26.1.1",
    "eslint": "9.39.5",
    "eslint-config-next": "16.2.10",
    "typescript": "6.0.3",
    "typescript-eslint": "8.64.0"
  }
}
