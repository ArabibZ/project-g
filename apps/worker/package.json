{
  "name": "@project-g/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "wrangler types src/worker-configuration.d.ts && tsc -p tsconfig.json",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "@project-g/shared": "workspace:*",
    "@supabase/supabase-js": "2.110.7",
    "hono": "4.12.31",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "wrangler": "4.112.0"
  }
}
